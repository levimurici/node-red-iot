"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const globals_1 = require("../globals");
class NodeRedContext {
    constructor(node) {
        this.node = node;
        this.node = node;
    }
    get(location, property, message) {
        if (location === 'msg') {
            if (!message)
                return;
            return globals_1.RED.util.getMessageProperty(message, property);
        }
        const { key, store } = globals_1.RED.util.parseContextStore(property);
        const context = this.node.context()[location];
        return context.get(key, store);
    }
    set(val, location, property, message) {
        const { key, store } = globals_1.RED.util.parseContextStore(property);
        switch (location) {
            case 'none':
                break;
            case 'flow':
            case 'global':
                this.node.context()[location].set(key, val, store);
                break;
            case 'msg':
            default:
                if (!message)
                    return;
                globals_1.RED.util.setObjectProperty(message, key, val);
                break;
        }
    }
}
exports.default = NodeRedContext;
